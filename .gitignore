node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
/dist

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*

# Internal planning docs & data (not for public repo)
/docs/
/data/

# Generated registry — built into the site's static dir, served by GitHub Pages
# at /svelte-aria/r where the CLI fetches it. Regenerated by `build:site` in CI.
/registry/dist/
/static/r/
